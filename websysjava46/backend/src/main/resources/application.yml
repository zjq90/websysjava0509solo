server:
  port: 8080

spring:
  application:
    name: flower-shop-admin

  datasource:
    url: jdbc:h2:mem:flower_shop;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.H2Dialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

springfox:
  documentation:
    enabled: true
    swagger-ui:
      enabled: true

logging:
  level:
    com.flower: DEBUG
    org.hibernate.SQL: DEBUG
